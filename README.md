# Beauty Master Bot + Website

Демо-проект для бьюти-сферы: Telegram-бот записи + сайт-визитка.

## Возможности

- услуги и цены;
- выбор нескольких услуг;
- свободные окна по дате, времени и графику;
- админ-панель `/admin`;
- подтверждение или отмена заявки;
- SQLite;
- статичный сайт в `site/`.

## Запуск бота

Создайте `.env`:

```env
BOT_TOKEN=your_token_here
ADMIN_ID=123456789
DB_PATH=beauty_bot.db
```

Запуск:

```powershell
python -m venv .venv
.venv\Scripts\activate
pip install -r requirements.txt
python main.py
```

## Админ-панель

```text
/admin
```

Админ может добавлять, менять и удалять свободные окна. После заявки слот становится `blocked`, после подтверждения — `booked`, после отмены снова свободен.

## Сайт

Открыть:

```text
site/index.html
```

Менять данные:

```text
site/data.js
```

Там лежат название, город, услуги, цены, мастера, портфолио, контакты, Telegram, Instagram и цвет.

## Основные файлы

- `main.py` — запуск бота;
- `handlers.py` — сценарии бота;
- `database.py` — SQLite;
- `keyboards.py` — кнопки;
- `data.py` — данные бота;
- `site/` — сайт-визитка.

## Важно

Проект демонстрационный. Фото, отзывы, контакты, цены и ссылки нужно заменить под реального клиента.
